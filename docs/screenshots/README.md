# Screenshots (placeholders)

Add product screenshots here for reviewers and documentation.

Suggested filenames (PNG or WebP):

- `dashboard.png` — Command center with KPIs and charts
- `devices.png` — Device cards and connection timeline
- `analysis.png` — Risk narrative, what/why/how serious, do’s and don’ts
- `history.png` — Log table with filters

Link them from the root `README.md` once captured, for example:

```markdown
![Dashboard](docs/screenshots/dashboard.png)
```

Until images exist, the README uses text placeholders so the layout stays review-friendly.
